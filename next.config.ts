import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  images: {
    // Allow Next.js Image optimisation for external sources if needed later
    remotePatterns: [],
  },
};

export default nextConfig;
